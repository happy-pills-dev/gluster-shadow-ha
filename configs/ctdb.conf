# /etc/ctdb/ctdb.conf — gluster-shadow-ha template
# Replace <NODE_IP> with this node's IP (not the VIP)

[logging]
  location = file:/var/log/ctdb/ctdb.log
  log level = NOTICE

[cluster]
  transport = tcp
  node address = <NODE_IP>

[legacy]
  lmaster role = yes
